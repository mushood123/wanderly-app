import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  emptyContainer: {
    borderRadius: 10,
    padding: 20,
    borderWidth: 2,
    borderColor: 'red',
  },
  container: {
    borderRadius: 10,
    padding: 20,
    borderWidth: 1,
    borderColor: '#FFFFFF',
  },
  text: {
    fontSize: 18,
    color: 'white',
  },
  textInput: {
    fontSize: 18,
    color: '#294f8e',
  },
});

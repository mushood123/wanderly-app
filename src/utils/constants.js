import Config from 'react-native-config';

export const { STRIPE_PUBLISHABLE_KEY } = Config;

console.log('STRIPE_PUBLISHABLE_KEY', STRIPE_PUBLISHABLE_KEY);
console.log('STRIPE_PUBLISHABLE_KEY', Config);
